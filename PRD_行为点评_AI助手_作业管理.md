# 北大附小 AI 德育系统 — 产品需求文档（PRD）

> **版本**：v1.0 &nbsp;&nbsp; **日期**：2026-04-30 &nbsp;&nbsp; **范围**：行为点评 · AI点评助手 · 作业管理

---

## 1. 产品概述

### 1.1 背景
北大附小 AI 德育系统（"九章爱学"）面向班主任与授课教师，提供"四维度"（快乐 · 进取 · 儒雅 · 大气）行为评价工具。系统分为**大屏端**（教室展示）和**平板端/手机端**（教师日常操作）两个前端，通过 WebSocket 实时同步点评数据。

### 1.2 用户角色

| 角色 | 权限范围 | 典型场景 |
|------|---------|---------|
| **班主任** | 管辖班级 **所有学科** 的点评、排行榜、作业管理 | 日常德育评价、班会展示 |
| **授课教师** | 仅自己所教学科，且仅能操作自己 **授课班级** 的数据 | 语文老师仅对授课班级做语文相关点评/作业 |

### 1.3 技术架构
- **大屏端**：Vue 3 + Vite，部署于 `/deyu/deyu-bigscreen/`，3000 端口
- **平板端**：Vue 3 + Vite + Pinia，部署于 `/deyu/tablet/`，3001 端口
- **共享包**：`packages/shared`（类型、常量、composables）
- **实时通信**：WebSocket，消息类型 `review_sync` / `homework_submit` / `ranking_update`
- **嵌套关系**：平板端"行为点评"页通过 iframe（`?embed=1`）嵌套大屏端页面，保持样式 100% 一致

---

## 2. 登录与身份

### 2.1 登录方式
平板端采用 **手机号 + 短信验证码** 登录（备选：密码登录）。

#### 登录流程
1. 教师打开平板端 → 展示登录页（品牌头 + 快捷账号卡片 + 表单）
2. 支持**快捷账号切换**：已登录过的账号以卡片展示（姓名 + 班级），点击切换
3. 输入手机号 → 获取验证码（60s 倒计时）→ 输入验证码 → 登录
4. 登录成功后，系统通过手机号匹配教师身份，返回以下关键信息：

```typescript
interface Teacher {
  id: string
  name: string          // "张明"
  phone: string         // "138****8888"
  role: '班主任' | '授课教师'   // 角色（规划）
  subject?: Subject     // 授课学科（授课教师必填）
  classes: ClassInfo[]  // 关联班级列表
}
```

### 2.2 身份匹配后的自动行为
- **默认班级**：自动选中 `classes[0]`（通常为班主任管辖班级）
- **班级切换器**：全局顶部显示班级下拉选择器，可切换当前操作班级
- **学科权限**：
  - 班主任 → 可查看/操作当前班级所有学科的点评和作业
  - 授课教师 → 仅能操作自己学科相关内容（作业发布默认锁定为自己学科，点评不限学科但会有学科上下文标记）
- **教师名回显**：各页面自动回填当前教师姓名（如作业创建者、点评操作者），无需手动输入

### 2.3 登出
右上角用户头像 → 下拉菜单 → "退出登录" → 清空 Pinia 状态 + 跳转登录页。

---

## 3. 行为点评（ReviewPage）

### 3.1 页面结构
平板端 ReviewPage 采用 **iframe 嵌套大屏端** 方案，保持大屏视觉一致性。

```
┌─────────────────────────────────────────────┐
│ PageHeader: "行为点评"   [WebSocket状态] [班级] │
│ ┌─────┐ ┌──────┐ ┌──────┐                    │
│ │ 点评 │ │排行榜│ │交作业│  (Tab 切换)          │
│ └─────┘ └──────┘ └──────┘                    │
├─────────────────────────────────────────────┤
│                                             │
│  iframe: 大屏端对应页面 ?embed=1             │
│                                             │
└─────────────────────────────────────────────┘
```

### 3.2 Tab 页签

| Tab | iframe 地址 | 功能说明 |
|-----|-----------|---------|
| 点评 | `/review?embed=1` | 四维度指标点评面板、学生/小组选择、加减分操作 |
| 排行榜 | `/ranking?embed=1` | 个人/小组排行、维度筛选、时间范围切换 |
| 交作业 | `/homework?embed=1` | 大屏端作业提交看板（学生扫码交作业 → 加分） |

### 3.3 embed 模式处理
- 大屏端路由守卫检测 `?embed=1`，跳过登录验证（平板端已登录）
- TopNav 保持可见，但**隐藏右上角用户头像/登出**（避免在 iframe 内操作登出）
- 班级选择器在 embed 模式下仍可用（含授课班级，如"（授课）三年级5班"）

### 3.4 班级切换
- 班主任可看到：自己管辖的班级（如 "三年级1班"、"三年级2班"）
- 授课教师可看到：授课班级（如 "（授课）三年级5班"、"（授课）四年级3班"）
- 切换班级 → iframe 内容随之刷新（通过 Pinia store 联动或 iframe postMessage）

### 3.5 四维度指标体系
系统采用北大附小自研的四维度德育评价体系，共 **89 项** 行为指标：

| 维度 | Key | 指标数 | 示例 |
|------|-----|-------|------|
| 快乐（身心健康） | happy | 25 项 (h01-h25) | 不挑食(+1)、危险举动(-2)、坚持运动(+3) |
| 进取（创新素养） | progress | 20 项 (p01-p20) | 按时完成作业(+1)、扰乱课堂(-2)、主动举手(+1) |
| 儒雅（儒雅品格） | elegant | 24 项 (e01-e24) | 礼貌问好(+1)、打架(-2)、每日阅读(+3) |
| 大气（责任担当） | generous | 20 项 (g01-g20) | 认真值日(+1)、乱扔垃圾(-1)、跨文化交流(+3) |

每项指标包含：`id`、`label`（行为描述）、`type`（positive/negative）、`points`（分值 ±1/±2/±3/±5）。

### 3.6 数据同步
点评提交后，通过 WebSocket 广播 `review_sync` 消息：
```typescript
{
  type: 'review_sync',
  payload: {
    studentIds: string[]
    indicators: { id: string; points: number; dimension: string }[]
    totalPoints: number
    teacherId: string
    timestamp: number
  }
}
```
大屏端实时接收 → 更新排行榜、学生积分、动画播报。

---

## 4. AI 点评助手（VoiceReviewPage）

### 4.1 产品定位
面向教师的**自然语言交互式德育评价工具**。教师通过文字或语音描述学生行为，AI 自动识别学生、匹配行为指标、给出评分建议，并在负面行为时提供德育处置指导。

### 4.2 页面结构

```
┌──────────────────────────────────────────┐
│ PageHeader: "AI点评助手"   [WebSocket状态] │
├──────────────────────────────────────────┤
│ ┌──────────────────────────────────────┐ │
│ │ 欢迎横幅                [历史记录 ›]  │ │
│ │ "你好！我是AI德育助手"      🤖        │ │
│ └──────────────────────────────────────┘ │
│                                          │
│ 💡 猜你想说（始终展示）                    │
│  [同学们认真听讲] [全班表现很好] [...]     │
│                                          │
│ ── 聊天消息流 ──                          │
│ 用户: "全班上课认真，婉云主动举手"          │
│ AI: "好的，识别到以下点评"                 │
│ ┌ 点评明细卡片 ─────────────────────┐    │
│ │ [全班16人 ▾] [上课遵守纪律... ▾] +1 🪙│ │
│ │ [徐婉云 ▾]  [课堂主动举手... ▾]  +1 🪙│ │
│ │        [取消]  [确定]                 │ │
│ └────────────────────────────────────┘  │
│ 💡 德育建议（负面行为时展示）              │
│                                          │
├──────────────────────────────────────────┤
│ 🎙️ [描述学生表现...         ] [发送]     │
└──────────────────────────────────────────┘
```

### 4.3 AI Agent 能力设计

#### 4.3.1 输入分析管线

```
教师输入文本
    │
    ▼
① 分句（逗号、句号等标点切分）
    │
    ▼
② 每句独立分析：
   ├── 学生识别（全名 → 部分名 → 同姓候选）
   ├── 全班意图检测（"全班"/"大家"/"同学们"等关键词）
   └── 行为指标匹配（关键词规则 → 标签文本匹配 → 置信度评分）
    │
    ▼
③ 按置信度分流到 4 种对话路径
```

#### 4.3.2 四种对话路径

**路径 A — 高置信度匹配（学生 ✓ + 指标 ✓）**

直接生成点评卡片，无需追问。

- 输入："全班上课认真，婉云主动举手"
- AI："好的，识别到以下点评" + 展示卡片
- 如果是负面行为，追加 **德育指导建议**

**路径 B — 学生不明确（学生 ✗ + 指标 ✓）**

AI 主动询问学生身份，提供候选选项。

- 输入："小明上课很认真"
- AI："我在班级名单中没有找到'小明'，您是指以下哪位同学？"
- 展示候选 chips：[马子扬] [毛昱涵] [选择其他同学]

候选逻辑：
1. 优先展示**同姓**学生
2. 其次展示**名字有交集字符**的学生
3. 最多展示 6 个候选
4. 提供"选择其他同学"按钮 → 打开多选学生弹窗
5. 提供"全班同学"按钮 → 直接应用全班

**路径 C — 行为不明确（学生 ✓ + 指标 ✗）**

AI 主动推荐行为指标，引导教师选择。

- 输入："婉云今天表现不错"
- AI："婉云的行为，您是指以下哪种情况？"
- 展示候选 chips：[上课遵守纪律 +1] [主动举手发言 +1] [选择其他指标]

候选逻辑：
1. 优先展示**关键词部分匹配**的指标（得分 > 0 但 < 阈值）
2. 如果无候选 → 推荐**常用指标**（p04 上课遵守纪律、p07 主动举手、p01 按时作业、e11 认真倾听、e09 礼貌问好、g07 集体荣誉、h15 认真做操）
3. 提供"选择其他指标" → 打开完整指标选择弹窗（89 项，按四维度分组）

**路径 D — 全部不明确（学生 ✗ + 指标 ✗）**

引导教师提供更具体的描述。

- 输入："有个同学不太好"
- AI："请描述学生的表现，我来帮您记录。比如'小明上课认真听讲'、'全班今天值日做得好'"
- 展示 chips：[选择同学+指标] [创建自定义指标]

#### 4.3.3 关键词 → 指标匹配规则

系统内置 **34 条关键词规则**，覆盖四个维度的高频行为：

```
关键词组                        → 指标ID    权重
['上课','认真','遵守纪律']      → p04       2
['举手','发言','回答','积极发言'] → p07       3
['打架','骂人','欺负','欺凌']    → e02       3
['作业','按时','完成','交作业']   → p01       2
['吃饭','不挑食','光盘']         → h11       2
...
```

匹配算法：
1. 遍历每条规则，计算输入文本命中的关键词 × 权重 = 得分
2. 取最高分规则，若 ≥ 2 分 → 高置信度匹配
3. 若 < 2 分 → 收集所有得分 > 0 的规则作为候选

#### 4.3.4 多轮对话上下文管理

```typescript
interface PendingContext {
  type: 'need-student' | 'need-indicator' | 'need-both'
  originalText: string
  matchedStudents?: Student[]
  matchedIndicator?: Indicator | null
  candidateStudents?: Student[]
  candidateIndicators?: Indicator[]
  isClassWide?: boolean
}
```

- 当 AI 提出追问后，将 `PendingContext` 存入状态
- 教师的下一次输入（文字或点击 chip）优先在上下文内解析
- 解析成功 → 清除上下文 + 生成点评卡片
- 解析失败 → 继续追问或当作全新输入

#### 4.3.5 负面行为德育指导

当识别到负面行为指标时，AI 在点评卡片之后自动追加一条**暖黄色指导建议气泡**（💡 德育建议），内容覆盖 17 种常见负面场景：

| 指标 | 指导建议摘要 |
|------|------------|
| e02 打架/言语伤害 | 分开当事人 → 安抚情绪 → 了解经过 → 联系校医和家长 → 保留就医单据 |
| e03 严重欺凌 | 立即制止 → 检查伤情 → 通知德育处和家长 → 填写安全记录表 |
| h09 情绪发泄 | 控场隔离 → 安静空间冷静 → 沟通引导 → 必要时联系心理老师 |
| p05 课堂扰乱 | 暂停信号提醒 → 冷静角 → 课后沟通了解困扰 |
| p02 作业未完成 | 课后谈话 → 了解原因 → 针对性帮助 |
| h02 危险行为 | 确认安全 → 检查伤情 → 提醒全班 → 高频行为需联系家长 |
| g10 乱扔垃圾 | 环保小卫士轮值 → 培养环保意识 |
| ... | ... |

### 4.4 点评卡片交互

每条点评行包含三个可编辑区域：

| 区域 | 显示 | 交互 |
|------|------|------|
| 学生 | "全班16人" / "徐婉云" / "徐婉云等5人" | 点击 → 打开学生多选弹窗 |
| 指标 | "上课遵守纪律，不影响其他同学学习" | 点击 → 打开指标选择弹窗（89项） |
| 分值 | "+1 🪙" / "-2 🪙" | 自动跟随指标 |

- 学生多选弹窗：4 列头像网格、全选/清空、已选计数
- 指标选择弹窗：按四维度分组，正面/负面不同底色，当前选中高亮

### 4.5 点评提交

点击"确定" → 展开每行为每个学生生成一条 `VoiceReviewResult` → 调用 `voiceReviewStore.confirmAll()` → WebSocket 广播到大屏端 → 卡片标记"✅ 已提交"。

### 4.6 历史记录抽屉

点击欢迎横幅右侧"历史记录 ›" → 从右侧滑出抽屉。

- **日期范围选择**：本周 / 上周 / 自定义（两个 date input）
- **统计**：显示筛选结果总条数
- **按天分组**：今天 / 昨天 / X月X日，每组显示条数
- **每条记录**：时间 + 💬原始对话文本 + 学生标签 + 指标描述 + 分值
- **展开全部聊天**：每个日期分组旁有"展开全部聊天"按钮 → 弹窗展示当天完整对话流（用户消息 + AI 回复，气泡样式）

### 4.7 快捷建议（猜你想说）

位于欢迎横幅下方，**始终展示**（不随消息出现而隐藏）。

- 预设建议：["同学们认真听讲，积极发言", "全班表现很好", "班级今日卫生非常干净"]
- 点击 → 自动填入输入框并发送

### 4.8 班级与学科上下文

- 页面顶部显示当前班级（从全局 store 获取）
- AI 解析学生姓名时，从当前班级的学生名单中匹配
- 班主任场景：不限学科
- 授课教师场景：点评结果自动标记为教师所教学科（规划，当前未限制）

---

## 5. 作业管理（HomeworkPage）

### 5.1 页面结构

```
┌─────────────────────────────────┐
│ PageHeader: "作业管理"           │
├─────────────────────────────────┤
│ ┌────┐ ┌────┐ ┌────┐           │
│ │ 5  │ │87% │ │92% │           │
│ │本周 │ │提交 │ │按时 │  概览卡片  │
│ └────┘ └────┘ └────┘           │
│                                 │
│ [今日] [本周] [本学期] [全部]     │  ← 时间筛选
│ [全部] [语文] [数学] [英语] [其他]│  ← 学科筛选
│                                 │
│ ┌───────────────────────────┐  │
│ │ 语文  古诗三首背诵默写       │  │
│ │ 张老师发布 · 按时10 补交1 未3│  │  ← 作业卡片列表
│ └───────────────────────────┘  │
│ ...                             │
│                           [+]   │  ← 浮动发布按钮
└─────────────────────────────────┘
```

### 5.2 概览统计卡片

| 指标 | 计算逻辑 |
|------|---------|
| 本周作业数 | `createdAt` 在本周一 00:00 之后的作业总数 |
| 平均提交率 | 所有作业的 `(已提交+迟交) / 总人数` 的平均值 |
| 平均按时率 | 所有作业的 `按时提交 / (按时+迟交)` 的平均值 |

### 5.3 筛选体系

**时间筛选**（胶囊 Tab，互斥）：

| 选项 | 筛选逻辑 |
|------|---------|
| 今日 | `createdAt` ≥ 今天 00:00 |
| 本周 | `createdAt` ≥ 本周一 00:00（默认） |
| 本学期 | `createdAt` ≥ 5个月前 |
| 全部 | 不筛选 |

**学科筛选**（标签 Tab，互斥）：

| 选项 | 筛选逻辑 |
|------|---------|
| 全部（默认） | 不筛选学科 |
| 语文 / 数学 / 英语 / 其他 | `subject === 选中学科` |

筛选结果按 `createdAt` 倒序排列。

### 5.4 班级筛选切换
- 作业数据按 `classId` 关联班级
- 切换全局班级选择器 → 作业列表自动刷新为对应班级的作业
- 授课教师仅看到自己学科的作业（通过 `subject` 过滤）
- 班主任可看到所有学科的作业

### 5.5 作业卡片

每张作业卡片展示：
- **学科标签**（颜色编码：语文红、数学蓝、英语绿、其他灰）
- **截止时间**（格式：`M-DD HH:MM截止`，已过期标红 "已截止"）
- **作业标题**（最多 50 字）
- **发布信息**（"X老师发布于 M-DD HH:MM"）
- **提交统计**：按时提交 N（绿）· 补交 N（橙）· 未交 N（灰）

点击卡片 → 打开作业详情弹窗。

### 5.6 发布作业

点击右下角浮动按钮"+" → 弹出底部弹窗。

**表单字段**：

| 字段 | 类型 | 规则 |
|------|------|------|
| 学科 | 单选 chip（语文/数学/英语/其他） | 必填；授课教师默认锁定为自己学科 |
| 作业标题 | 文本输入 | 必填，最多 50 字 |
| 截止时间 | datetime-local | 默认明天 17:00 |

提交后：
- 创建新 `Homework` 对象，`createdBy` 自动填充当前教师名
- `classId` 自动取当前选中班级
- `totalStudents` 取当前班级学生数
- `submissions` 初始化为所有学生 `status: 'missing'`
- 新作业插入列表头部

### 5.7 作业详情

点击卡片 → 底部弹窗展示详情。

**信息区**：
- 学科标签 + 状态标签（进行中/已结束）
- 作业标题
- 发布人 · 截止时间

**统计区**：
- 提交率（百分比）
- 按时提交人数（绿色）
- 迟交人数（橙色）
- 未交人数（红色）

**学生名单**：
- 滚动列表，每行：学生姓名 · 提交时间 · 状态标签
- 状态标签样式：已提交（绿底）/ 迟交（橙底）/ 未提交（红底）

### 5.8 作业数据模型

```typescript
interface Homework {
  id: string
  title: string              // 作业标题
  subject: Subject           // 学科（语文/数学/英语/其他）
  deadline: string           // 截止时间 ISO string
  classId: string            // 关联班级 ID
  createdBy: string          // 发布教师姓名（自动填充）
  createdAt: string          // 创建时间 ISO string
  status: 'active' | 'ended' // 是否已截止
  totalStudents: number      // 班级总人数
  submissions: HomeworkSubmission[]
}

interface HomeworkSubmission {
  studentId: string
  studentName: string
  submittedAt?: string       // 提交时间
  isOnTime?: boolean         // 是否在截止前
  status: 'submitted' | 'late' | 'missing'
}
```

---

## 6. 全局能力

### 6.1 班级切换器

所有功能页面共享全局班级切换器（TopNav / PageHeader 内），切换后影响：
- 行为点评：iframe 内大屏端内容切换
- AI 点评助手：学生名单切换（姓名匹配基于当前班级）
- 作业管理：作业列表按 classId 过滤

### 6.2 WebSocket 连接状态

每个功能页面顶部显示连接状态指示灯：
- 绿色 "已连接" — WebSocket 已建立
- 红色 "未连接" — 断开状态

### 6.3 导航结构（平板端侧边栏）

```
🏠 首页
⭐ 行为点评
🤖 AI点评助手
📝 作业管理        ← 紧跟在 AI 点评助手下方
📊 德育报告
⚠️ 预警中心
💬 AI电话亭
👥 班级管理
```

---

## 7. 数据流与系统集成

### 7.1 点评数据流

```
教师（平板端）
    │
    ├─ 行为点评 Tab ──→ iframe 内大屏端操作
    │                      │
    ├─ AI 点评助手 ──→ parseVoiceReview / analyzeInput
    │                      │
    │                      ▼
    │              voiceReviewStore.confirmAll()
    │                      │
    └──────────────────────┤
                           ▼
                   WebSocket broadcast
                   type: 'review_sync'
                           │
                           ▼
                   大屏端实时更新
                   (排行榜 / 积分动画 / 播报)
```

### 7.2 作业数据流

```
教师发布作业 → homework store → 作业列表
学生提交作业 → WebSocket homework_submit → 更新 submission 状态
大屏端交作业看板 → 扫码确认 → 触发加分（p01 按时完成作业 +1）
```

---

## 8. 附录

### 8.1 学科配色

| 学科 | 色值 |
|------|------|
| 语文 | `#E53935`（红） |
| 数学 | `#1E88E5`（蓝） |
| 英语 | `#43A047`（绿） |
| 其他 | `#78909C`（灰） |

### 8.2 设计色彩规范

| 用途 | 色值 |
|------|------|
| 主色（校徽红） | `#EE595B` |
| AI 助手主题紫 | `#7C5CFC` |
| 页面背景 | `#FFF8F7` |
| 卡片边框 | `#FFD6D6` |
| 正面行为分值 | 紫色（`#7C5CFC`） |
| 负面行为分值 | 红色（`#F44336`） |
| 德育建议气泡 | 暖黄色渐变（`#FFF8E1` → `#FFF3E0`） |

### 8.3 四维度指标完整列表

详见 `packages/shared/constants/index.ts` 中的 `MORAL_DIMENSION_INDICATORS` 常量，以及源文件 `行为指标规则.csv`。

---

## 9. 页面权限控制

### 9.1 模块可见性

| 模块 | 班主任 | 授课教师 | 说明 |
|------|--------|---------|------|
| 行为点评（点评/排行榜/交作业） | 可见 | 可见（仅自己学科） | 授课教师仅操作授课班级+授课学科 |
| AI点评助手 | 可见 | 可见 | 授课教师仅操作授课班级 |
| 作业管理 | 可见 | 可见（仅自己学科） | 授课班级锁定学科，班主任班级可管理所有学科 |
| **德育报告** | **可见** | **不可见** | 仅班主任角色可见 |
| **预警中心** | **可见** | **不可见** | 仅班主任角色可见 |
| **班级管理** | **可见** | **不可见** | 仅班主任角色可见 |

### 9.2 数据范围规则

- **德育报告 / 预警中心 / 班级管理** 的数据范围限定为：**班主任角色管辖的班级**（`classRoles[classId] === 'homeroom'`），**不包括授课班级**。
- 这三个模块的班级选择器仅展示班主任班级，不展示授课班级。
- 授课教师（仅有 `teaching` 角色，无 `homeroom` 角色的教师）在导航中不显示这三个入口。

### 9.3 班级筛选器独立性

各模块的班级筛选器互相独立，切换一个模块的班级不影响其他模块：

| 模块 | 班级筛选状态位置 | 默认值 |
|------|-----------------|--------|
| 作业管理 | `homeworkStore.selectedClassId` | 第一个班级（含授课班级） |
| 德育报告 | `ReportPage` 组件本地 `ref` | 第一个班主任班级 |
| 预警中心 | `WarningPage` 组件本地 `ref` | 第一个班主任班级 |
| 班级管理 | `ClassManagePage` 组件本地 `ref` | 第一个班主任班级 |
